# sysdx backend + worker
fastapi>=0.115,<0.120
uvicorn[standard]>=0.30,<0.36
sqlalchemy>=2.0,<2.1
pydantic>=2.7,<3.0
psycopg2-binary>=2.9,<3.0

# the parsing engine itself (installed from the repo root, see Dockerfile / dev setup)
-e ../..
